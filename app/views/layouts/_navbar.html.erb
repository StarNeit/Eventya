<nav class="shadow px-3 pt-[30px] bg-[#00488B] pb-5">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
        <div class="flex">
        <div class="flex-shrink-0 flex items-center">
            <%= link_to "ChatroomApp", root_path, class: "text-xl font-bold text-white cursor-pointer" %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
            <!--<% if user_signed_in? %>
            <%= link_to "Chatrooms", chatrooms_path, class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "New Chatroom", new_chatroom_path, class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium" %>
            <% end %>
            -->
        </div>
        </div>
        <div class="flex items-center">
        <% if user_signed_in? %>
            <span class="text-[#fcfcfc] mr-4">Signed in as <%= current_user.email %></span>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-red-500 hover:text-red-700 px-3 py-2 rounded-md text-sm font-medium" %>
        <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "text-[#fcfcfc] hover:text-gray-200 px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Login", new_user_session_path, class: "text-[#fcfcfc] hover:text-gray-200 px-3 py-2 rounded-md text-sm font-medium" %>
        <% end %>
        </div>
    </div>
    </div>
</nav>