<!DOCTYPE html>
<html>
  <head>
    <title>MChatroom</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <%# try the --prose mx-auto for center as well %>
  <body class="bg-gray-100" data-controller="notification">

    <%= render "layouts/navbar" %>

    <div class="container mx-auto px-4 py-6">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>
      <% if user_signed_in? %>
        <%= yield %>
      <% elsif !user_signed_in? && !devise_controller? %>
          <%= render "layouts/unauth" %>
      <% else %>
        <%= yield %>
      <% end %>
    </div>

    <!--
    <main class="container mx-auto mt-28 px-5 flex w-full h-full">
      <%= yield %>
    </main>
    -->

    <script src="https://cdn.jsdelivr.net/npm/blueimp-md5/js/md5.min.js"></script>
  </body>
</html>
